apiVersion: v1
kind: Service
metadata:
  name: monitor
  labels:
    app: monitor
spec:
  selector:
    app: monitor
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: LoadBalancer